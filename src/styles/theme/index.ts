import { colors } from "../colors";

// eslint-disable-next-line
export default {
    colors: {
        backgroundWhite: colors.BACKGROUNDWHITE,
        backgroundOrange: colors.BACKGROUNDORANGE,
        textColor: colors.TEXTCOLOR,
        gray: colors.GRAY,
        darkGrey: colors.DARKGREY,
        divider: colors.DIVIDER
    }
}